<h1>検索結果: <%= @search_reviews.count %>件</h1>
<h2>キーワード: "<%= params[:q][:title_or_body_cont] %>"</h2>
  <% if @search_reviews.present? %>
  <table class="table">
    <thead>
      <tr>
        <th>ユーザー</th>
        <th>タイトル</th>
        <th>本文</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @search_reviews.each do |review| %>
        <tr>
          <td>
            <%= link_to user_path(review) do %>
              <%= attachment_image_tag review.user, :profile_image, fallback: "default-image.jpg", size:'100x100' %>
              <%= review.user.name %>
            <% end %>
          </td>
          <td><%= link_to review.title, review_path(review) %></td>
          <td><%= review.body %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <% else %>
    <h2 class="text-center">お探しのレビューは見つかりませんでした</h2>
  <% end %>
