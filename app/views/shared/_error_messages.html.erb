<% if object.errors.any? %>
  <div id="error_explanation" class="alert alert-danger">
    <h4><%= pluralize(object.errors.count, "件エラー") %> が発生しました</h4>
    <ul>
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>